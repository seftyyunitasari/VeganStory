<!DOCTYPE html>
<html>
  <head>
    <title>VeganStory</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://use.fontawesome.com/aeeffffca1.js"></script>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">

  </head>
  <style>
   footer{
    text-align: center;
   }
  </style>
  <body>

    <div class="header justify-content-center">
      <ul class="nav">
        <li class ="nav-item">
          <a class="nav-link active" href="<%= products_path %>">
            <strong>VeganStory</strong>
          </a>
        </li>
        <li class ="nav-item">
          <a class="nav-link active" href="<%= products_path %>">
            <p>Products<p>
          </a>
        </li>
        <% if logged_in? %>
          <li class="nav-item">
            <a class="nav-link active" href="<%= user_path(current_user.id) %>">
              <p>Account<p>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="<%= carts_path %>">
              <p>Cart<p>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="<%= favourites_path %>">
              <p>Favourite<p>
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" href="<%= order_details_path %>">
              <p>Order History<p>
            </a>
          </li>
          <% if current_user.is_admin? %>
            <li class="nav-item">
              <a class="nav-link active" href="<%= admin_products_path %>">
                <p>Admin products<p>
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="<%= admin_users_path %>">
                <p>Admin Users<p>
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="<%= admin_order_details_path %>">
                <p>Admin Order History<p>
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link active" href="<%= comments_path %>">
                <p>Admin Comments<p>
              </a>
            </li>
          <% end %>
          <li class="nav-item">
            <%= button_to "Log out", session_path(current_user.id), method: :delete %>
          </li>
        <% else %>
        <li class="nav-item">
          <a class="nav-link active" href="<%= new_user_path %>">
            <p>Create Account<p>
           </a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" href="<%= new_session_path %>">
            <p>Log in<p>
           </a>
        </li>
        <% end %>
      </ul>
    <div>

    <% flash.each do |type, message| %>
        <%= content_tag(:div, message, class:"#{type}") %>
    <% end %>

    <%= yield %>

    <footer>
      <div class="footer-content">
        <p><a href="#"><i class="fa fa-facebook"></i></a>  <a href="#"><i class="fa fa-linkedin"></i></a>   <a href="#"><i class="fa fa-youtube"></i></a>   <a href="#"><i class="fa fa-twitter"></i></a>   <a href="#"><i class="fa fa-instagram"></i></a></p>
      </div>
      <div class="footer-bottom">
        <p>copyright &copy;2020 VeganStory</p>
      </div>
    </footer>
  </body>
</html>
